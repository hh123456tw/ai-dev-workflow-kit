# Copy to ../.local/models.ps1 and verify values with OpenCode /models.
# Do NOT put API keys here. Verified 2026-09-15 via `opencode models`.
$env:OPENCODE_PRIMARY_MODEL = "openai/gpt-5.6-sol"
$env:OPENCODE_WORKER_MODEL  = "deepseek/deepseek-v4-flash"
