# Local-only files

This directory is intentionally ignored except for this README.

On each computer create:

- `.local/models.ps1` on Windows, or
- `.local/models.sh` on macOS/Linux

from the examples in `scripts/`.

Never commit API keys, tokens, provider credentials, `.env`, or secret files.
