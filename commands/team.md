---
description: TEAM MODE entry. GPT-5.6 team-lead + Matt Pocock + Ensemble + DeepSeek workers, parallel. For hackathons, MVPs, demos, prototypes.
agent: team-lead
model: openai/gpt-5.6-sol
---

Run TEAM MODE for this request: $ARGUMENTS

Act as team-lead: grill-lite, MVP scope, to-spec, to-tickets as vertical
slices, dependency DAG, spawn every safe ready ticket concurrently via Ensemble
(2-3 workers, one ticket = one branch = one worktree), process results as they
arrive, integrate, review, qa, demo gate. Never run tickets sequentially when
dependencies allow parallel. Never fake results.
