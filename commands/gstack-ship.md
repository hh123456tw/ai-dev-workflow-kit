---
description: Shared specialist toolbox (gstack ship). Runs the gstack ship workflow only.
---

Use ONLY the gstack `ship` skill from the installed gstack skills directory for
this request: $ARGUMENTS. Verify tests, review the diff, then follow the skill
for versioning, changelog, commit, push, and PR. Never push or merge without
the required validation passing.
